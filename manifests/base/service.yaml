apiVersion: v1
kind: Service
metadata:
  name: datactive-web
  labels:
    app: datactive-web
spec:
  selector:
    app: datactive-web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
