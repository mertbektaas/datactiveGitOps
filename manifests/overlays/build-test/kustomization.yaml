apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: build-test-20260806
resources:
  - ../../base
images:
  - name: harbor.datactive.net/datateam/datactive.web
    newTag: test-001
# Test ortamı: image'ı test registry'den çek (production'da bu patch yok)
patches:
  - target:
      kind: Deployment
      name: datactive-web
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: datactive-registry:5000/datateam/datactive.web:U2E-20260806-160000
