apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: build-20260807-build

resources:
  - "../../base"

images:
  - name: datactive-registry:5000/datateam/datactive.web
    newTag: "BUILD-20260807-124042"

configMapGenerator:
  - name: datactive-config
    behavior: merge
    literals:
      - DQLSchema=bugfix_test
      - CustomerMetadataSchema=bugfix_test.
      - ORACLEDataSchema=bugfix_test.
