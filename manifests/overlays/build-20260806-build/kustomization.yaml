apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: build-20260806-build

resources:
  - "../../base"

images:
  - name: datactive-registry:5000/datateam/datactive.web
    newTag: "BUILD-20260806-151732"

configMapGenerator:
  - name: datactive-config
    behavior: merge
    literals:
      - DQLSchema=auto_v3
      - CustomerMetadataSchema=auto_v3.
      - ORACLEDataSchema=auto_v3.
